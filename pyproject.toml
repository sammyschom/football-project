[project]
name = "football-ml-project"
version = "0.1.0"
description = "Football machine learning and data analysis project"
requires-python = ">=3.8"

dependencies = [
    "pandas",
    "duckdb",
    "requests",
]